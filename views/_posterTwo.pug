section.posterTwo
    .posterTwo__title(href="#")
        p چەمکەکان
    .posters

        each poster in posterTwo
            .poster
              a.poster__link(href=`/movies/${poster.slug}`)
                img.poster__img(src=`${poster.idPoster}`, alt="" loading="lazy")
                p.poster__titleFilm= poster.title