extends base

block content
    section.posterOne
        .posterOne__title(href="#")
            p دەرهێنەران
        .posters

            each poster in director
                .poster
                    a.poster__link(href=`/movies/${poster.slug}`)
                        img.poster__img(src=`/img/posters/${poster.photo}`, alt="")
                        p.poster__titleFilm= poster.title