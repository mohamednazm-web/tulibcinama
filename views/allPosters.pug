extends base

block content
    section.posterOne
        .posterOne__title(href="#")
            p هەموو
        .posters

            each poster in allPosters
                .poster
                 a.poster__link(href=`/movies/${poster.slug}`)
                   img.poster__img(src=`/img/posters/${poster.photo}`, alt="Poster 1" loading="lazy")
                   p.poster__titleFilm= poster.title