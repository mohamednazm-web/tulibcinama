extends base

block content
    include _placeOfImage
    section.posterOne
        .posterOne__title(href="#")
            p نواندنکاران
        .posters
            each poster in actor
                .poster_container_nice
                    .card_poster_nice
                        a(href='#')
                            .img1(style=`background-image:url(${poster.idPoster});`)
                            //.img2
                            .text= poster.description
                        a(href='#')
                            .catagory= poster.typeActor
                                i.fas.fa-film
                        a(href='#')
                            .views= poster.hasViewd
                                i.far.fa-eye
                    .card_poster_other
                        p.poster__titleFilm= poster.title
